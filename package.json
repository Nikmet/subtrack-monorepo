{
    "name": "subtrack",
    "version": "0.1.0",
    "private": true,
    "workspaces": [
        "frontend",
        "server",
        "packages/contracts"
    ],
    "scripts": {
        "dev": "concurrently --kill-others-on-fail -n frontend,server -c cyan,magenta \"npm run dev --workspace frontend\" \"npm run dev --workspace server\"",
        "dev:frontend": "npm run dev --workspace frontend",
        "dev:server": "npm run dev --workspace server",
        "build": "npm run build --workspace frontend && npm run build --workspace server",
        "start:frontend": "npm run start --workspace frontend",
        "start:server": "npm run start --workspace server",
        "lint": "npm run lint --workspace frontend",
        "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
