{
    "version": 2,
    "buildCommand": "npm run prisma:generate",
    "routes": [
        {
            "src": "^/health$",
            "dest": "/api/index.ts"
        },
        {
            "src": "^/api/(.*)$",
            "dest": "/api/index.ts"
        }
    ]
}
